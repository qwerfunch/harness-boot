/**
 * Drive Phase C — Goal-level retrospective generator (v0.14.0 / F-119
 * — Stage 2).
 *
 * Fired once when the last feature in a Goal transitions to `done`.
 * Produces `_workspace/drive/goals/<G-NNN>/retro.md` with three
 * machine-filled sections (Goal summary · Feature breakdown · Halt
 * log) and two LLM-filled section stubs (Reviewer Reflection · Copy
 * Polish), mirroring the per-feature retro convention from
 * `src/ceremonies/retro.ts`.
 *
 * Idempotent — if `retro.md` already exists, the call is a no-op
 * (returns the path with `created: false`). Force regeneration is
 * not yet wired at the CLI layer; if needed, the caller can pass
 * `force: true`.
 *
 * Records a `goal_retro_written` event on `events.log` so metrics +
 * cross-goal learning have a per-goal anchor.
 *
 * @module drive/goalRetro
 */
import { appendFileSync, existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { State } from '../core/state.js';
import { goalArtifactDir, loadCheckpoint, progressLogPath } from './checkpoint.js';
import { readGoals } from './goalStore.js';
/** Returns the canonical retro path for a Goal. */
export function goalRetroPath(harnessDir, goalId) {
    return join(goalArtifactDir(harnessDir, goalId), 'retro.md');
}
/** Type guard — narrows an unknown value to a plain object. */
function isPlainObject(value) {
    return value !== null && typeof value === 'object' && !Array.isArray(value);
}
/** Returns the current UTC timestamp formatted as `YYYY-MM-DDTHH:MM:SSZ`. */
function nowIso(now = new Date()) {
    const yyyy = now.getUTCFullYear().toString().padStart(4, '0');
    const mm = (now.getUTCMonth() + 1).toString().padStart(2, '0');
    const dd = now.getUTCDate().toString().padStart(2, '0');
    const hh = now.getUTCHours().toString().padStart(2, '0');
    const mi = now.getUTCMinutes().toString().padStart(2, '0');
    const ss = now.getUTCSeconds().toString().padStart(2, '0');
    return `${yyyy}-${mm}-${dd}T${hh}:${mi}:${ss}Z`;
}
/**
 * Returns the first failed gate (if any) on a feature record.
 *
 * Walks the standard gate order (gate_0 → gate_5) and returns the
 * earliest entry whose `last_result === 'fail'`. Returns `null` when
 * no gate failed.
 */
function firstGateToFail(feature) {
    const order = ['gate_0', 'gate_1', 'gate_2', 'gate_3', 'gate_4', 'gate_5'];
    if (!isPlainObject(feature.gates)) {
        return null;
    }
    for (const g of order) {
        const entry = feature.gates[g];
        if (isPlainObject(entry) && entry.last_result === 'fail') {
            return g;
        }
    }
    return null;
}
/** Reads progress.log and counts each halt #N occurrence. */
function countHaltsFromProgressLog(harnessDir) {
    const path = progressLogPath(harnessDir);
    if (!existsSync(path)) {
        return { total: 0, perReason: {} };
    }
    const text = readFileSync(path, 'utf-8');
    const counts = {};
    let total = 0;
    for (const line of text.split('\n')) {
        const m = /HALT #(\d+) (\S+)/.exec(line);
        if (m === null) {
            continue;
        }
        const tag = `#${m[1]} ${m[2]}`;
        counts[tag] = (counts[tag] ?? 0) + 1;
        total += 1;
    }
    return { total, perReason: counts };
}
/**
 * Reads `_workspace/retro/F-N.md` and returns its first 5 lines as a
 * preview, or `null` when no per-feature retro exists.
 */
function featureRetroPreview(harnessDir, fid) {
    const path = join(harnessDir, '_workspace', 'retro', `${fid}.md`);
    if (!existsSync(path)) {
        return null;
    }
    const text = readFileSync(path, 'utf-8');
    const lines = text.split('\n').slice(0, 5);
    return lines.join('\n').trim();
}
/**
 * Mirrors the small `events.log` writer used elsewhere in drive (see
 * `halt.ts`). Append-only JSONL.
 */
function appendEvent(harnessDir, event) {
    const logPath = join(harnessDir, 'events.log');
    mkdirSync(dirname(logPath), { recursive: true });
    appendFileSync(logPath, `${JSON.stringify(event)}\n`, 'utf-8');
}
/**
 * Composes the retro markdown from spec + state + progress.log + the
 * checkpoint.
 */
function composeRetro(goalSpec, features, halts, iteration, elapsedSec, startedAt, completedAt, harnessDir) {
    const lines = [];
    lines.push(`# Goal Retro — ${goalSpec.id} · ${goalSpec.title}`);
    lines.push('');
    lines.push(`> auto-generated by drive's Phase C. Edit the LLM sections below; machine sections regenerate on \`--retro\`.`);
    lines.push('');
    // Goal summary (machine).
    lines.push('## Goal summary');
    lines.push('');
    lines.push(`- **Slug**: ${goalSpec.slug}`);
    lines.push(`- **Started at**: ${startedAt ?? 'unknown'}`);
    lines.push(`- **Completed at**: ${completedAt ?? 'unknown'}`);
    lines.push(`- **Iterations**: ${iteration}`);
    lines.push(`- **Elapsed (sec)**: ${elapsedSec}`);
    lines.push(`- **Features (${features.length})**: ${features.map((f) => f.id).join(', ')}`);
    lines.push('');
    // Feature breakdown (machine).
    lines.push('## Feature breakdown');
    lines.push('');
    lines.push('| Feature | Status | First gate to fail | Started | Completed |');
    lines.push('|---|---|---|---|---|');
    for (const f of features) {
        const ff = firstGateToFail(f) ?? '—';
        const started = (f.started_at ?? '').toString() || '—';
        const completed = (f.completed_at ?? '').toString() || '—';
        lines.push(`| ${f.id} | ${f.status} | ${ff} | ${started} | ${completed} |`);
    }
    lines.push('');
    // Per-feature retro previews (when available).
    let anyPreview = false;
    for (const f of features) {
        const preview = featureRetroPreview(harnessDir, f.id);
        if (preview === null) {
            continue;
        }
        if (!anyPreview) {
            lines.push('### Per-feature retro previews');
            lines.push('');
            anyPreview = true;
        }
        lines.push(`**${f.id}** (head 5 lines of \`_workspace/retro/${f.id}.md\`):`);
        lines.push('');
        lines.push('```');
        lines.push(preview);
        lines.push('```');
        lines.push('');
    }
    // Halt log (machine).
    lines.push('## Halt log');
    lines.push('');
    if (halts.total === 0) {
        lines.push('No halts during this goal — the loop ran clean.');
    }
    else {
        lines.push(`Total halts: **${halts.total}**`);
        lines.push('');
        lines.push('| Reason | Count |');
        lines.push('|---|---|');
        const sorted = Object.entries(halts.perReason).sort(([a], [b]) => a.localeCompare(b));
        for (const [reason, count] of sorted) {
            lines.push(`| ${reason} | ${count} |`);
        }
    }
    lines.push('');
    // Reviewer reflection (LLM stub).
    lines.push('## Reviewer Reflection');
    lines.push('');
    lines.push('_(pending — reviewer agent fills this in)_');
    lines.push('');
    lines.push('Suggested prompts:');
    lines.push('- What surprised you about how this goal unfolded?');
    lines.push('- Which halts were avoidable; which were structural?');
    lines.push('- One thing this goal would have benefitted from doing differently.');
    lines.push('');
    // Copy polish (LLM stub).
    lines.push('## Copy Polish');
    lines.push('');
    lines.push('_(pending — tech-writer agent polishes the prose above)_');
    lines.push('');
    return lines.join('\n');
}
/**
 * Generates `_workspace/drive/goals/<G-NNN>/retro.md` for the
 * supplied Goal.
 *
 * Idempotent — if the file already exists and `force` is `false`, the
 * call returns the existing path with `created: false`. The
 * `goal_retro_written` event is only appended on a fresh write.
 *
 * @returns A {@link GenerateGoalRetroResult} describing the outcome.
 */
export function generateGoalRetro(harnessDir, goalId, options = {}) {
    const path = goalRetroPath(harnessDir, goalId);
    const force = options.force ?? false;
    if (existsSync(path) && !force) {
        return { path, created: false, feature_count: 0, halt_count: 0 };
    }
    // Read the Goal definition from spec.yaml.
    const specPath = join(harnessDir, 'spec.yaml');
    const goals = existsSync(specPath) ? readGoals(specPath) : [];
    const goalSpec = goals.find((g) => g.id === goalId);
    if (goalSpec === undefined) {
        throw new Error(`drive goalRetro: ${goalId} not found in spec.yaml`);
    }
    // Pull the runtime feature records.
    const state = State.load(harnessDir);
    const features = [];
    for (const fid of goalSpec.feature_ids) {
        const f = state.getFeature(fid);
        if (f !== null) {
            features.push(f);
        }
    }
    // Pull iteration / elapsed / started_at from the checkpoint.
    const ck = loadCheckpoint(harnessDir);
    const iteration = ck?.execute.iteration ?? 0;
    const elapsedSec = ck?.execute.elapsed_sec ?? 0;
    const startedAt = ck?.execute.started_at ?? null;
    const completedAt = state.getGoal(goalId)?.completed_at ?? null;
    const halts = countHaltsFromProgressLog(harnessDir);
    const text = composeRetro(goalSpec, features, halts, iteration, elapsedSec, startedAt, completedAt, harnessDir);
    mkdirSync(dirname(path), { recursive: true });
    writeFileSync(path, text, 'utf-8');
    appendEvent(harnessDir, {
        ts: nowIso(options.now),
        type: 'goal_retro_written',
        goal_id: goalId,
        feature_count: features.length,
        halt_count: halts.total,
    });
    return {
        path,
        created: true,
        feature_count: features.length,
        halt_count: halts.total,
    };
}
//# sourceMappingURL=goalRetro.js.map