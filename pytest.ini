[pytest]
# Bare `python -m pytest` 가 design/oss-refs/ (gitignored 외부 OSS 레포 사본) 의
# 테스트들까지 수집하면서 collection error (의존성 부재) 로 exit 2 가 되는 문제
# 회피. gate_0 auto-detect 가 베어 pytest 를 호출하므로 testpaths 로 scope 고정
# 필요. F-026 에서 도입.
testpaths = tests/unit
