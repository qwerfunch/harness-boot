[package]
name = "sample-cli"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Minimal Rust CLI fixture for brownfield reconnaissance tests."

[dependencies]
clap = { version = "4.5", features = ["derive"] }

[[bin]]
name = "sample-cli"
path = "src/main.rs"
